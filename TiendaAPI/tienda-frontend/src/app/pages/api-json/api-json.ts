import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-api-json',
  imports: [RouterLink],
  templateUrl: './api-json.html',
  styleUrl: './api-json.scss',
})
export class ApiJson {
}

